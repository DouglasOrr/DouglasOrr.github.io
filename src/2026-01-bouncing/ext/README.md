# Bouncing balls simulator

A basic Newtonian mechanics simulator, mostly written in an afternoon, not made for serious purposes.

```sh
npm run dev  # host a development server

npm dist  # update the distributable in ../app
```
